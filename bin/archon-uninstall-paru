#!/usr/bin/env bash

XDG_DATA_HOME="${XDG_DATA_HOME:-$HOME/.local/share}"

# check if paru-bin is installed
if ! pacman -Qi paru-bin &> /dev/null; then
  sudo pacman -Rns --noconfirm paru-bin
fi

cd "$XDG_DATA_HOME"
rm -rf paru-bin
